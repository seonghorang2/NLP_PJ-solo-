# AGENTS.md

## 작업 방식 (Working Style)

- 변경 전 명세와 운영 문서를 먼저 확인한다.
- 바로 코드를 쓰지 말고, 먼저 구현/수정 계획을 세운다.
- 작은 단위로 나눠 모듈화하고 리뷰 가능한 형태로 작업한다.
- 코드는 단순하고 유지보수 가능한 구조를 우선한다.

---

## 프로젝트 우선순위

1. 정확성
2. 단순성
3. 유지보수성
4. 관심사의 분리

---

## 제약 조건

- MVP 범위만 구현한다.
- 불필요한 추상화, 과도한 인프라, 성급한 최적화를 금지한다.
- 초기 버전은 결정론적 분석을 기본으로 한다.
- LLM은 애매한 리뷰 subset에 대한 fallback으로만 사용한다.
- 단일 우선순위 점수는 MVP에서 사용하지 않는다.

---

## 저장소 경계 규칙

- 모노레포를 유지한다.
- 현재 대상 서비스는 `apps/review-insights/`에 한정한다.
- 타 팀 서비스와 코드/데이터 경계를 섞지 않는다.
- `shared/` 공통화는 실제 중복이 검증된 뒤에만 진행한다.

---

## 분석 규칙

- 한국어 단일 언어 기준 분석을 수행한다.
- 아래 리뷰는 분석에서 제외하고 메타데이터는 보존한다.
  - 의미 없는 저품질 리뷰
  - 단순 욕설-only 리뷰
  - 주요 내용이 한국어가 아닌 리뷰
- 카테고리 분류는 멀티 라벨을 기본으로 한다.
- 규칙 판정, LLM 판정, 최종 판정의 흔적을 저장 가능하게 설계한다.

---

## 데모 운영 모드 강제 규칙

- 제품 기능 범위는 단일 게임 리포트만 허용한다.
- 게임 비교 기능은 구현/노출하지 않는다.
- 사용자 조회 경로는 저장된 분석 스냅샷 조회만 수행한다.
- 사용자 조회 경로에서 Steam API/LLM 실시간 호출을 금지한다.
- 리뷰 수집/정규화/LLM fallback/집계는 오프라인 파이프라인에서만 실행한다.
- 데모는 전체 Steam이 아닌 고정 코호트 게임만 운영한다.
- “신규 리뷰 n개 이상 시 재분석”은 조건 판정 로직만 제공한다.
- 재분석 실행은 자동이 아니라 수동 실행으로 유지한다.

---

## 수정 전 확인 사항

- 저장소 구조를 먼저 파악한다.
- 기존 코드/문서 패턴을 우선 재사용한다.
- 큰 변경 전 어떤 파일을 어떻게 바꿀지 먼저 설명한다.

---

## 출력 기대 사항

- 기능/정책 변경 시 관련 문서를 함께 업데이트한다.
- 테스트한 내용과 테스트하지 않은 내용을 명확히 보고한다.
- 새로운 의존성/설정이 필요하면 설정 방법을 문서에 포함한다.

---

## 테스트 원칙

- 가능한 경우 관련 테스트를 실행한다.
- 테스트가 없다면 핵심 로직 최소 테스트를 추가한다.
