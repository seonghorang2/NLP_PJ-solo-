# AGENTS.md

## 작업 방식 (Working Style)

- 변경 작업 전에 반드시 제품 명세서를 먼저 읽는다.
- 코드를 바로 작성하지 말고, 먼저 간단한 구현 계획을 수립한다.
- 작은 단위로 나누어 모듈화하고, 리뷰 가능한 형태로 작업한다.
- 코드는 이해하기 쉽고 확장 가능하도록 유지한다.

---

## 프로젝트 우선순위 (Project Priorities)

1. 정확성 (Correctness)
2. 단순성 (Simplicity)
3. 유지보수성 (Maintainability)
4. 관심사의 명확한 분리 (Separation of Concerns)
5. 내부 도구로서 적절한 사용자 경험 (Reasonable UX)

---

## 제약 조건 (Constraints)

- MVP 범위만 구현한다.
- 불필요한 추상화를 만들지 않는다.
- 명확한 필요가 없는 한 복잡한 인프라를 도입하지 않는다.
- 성급한 최적화를 피한다.
- 초기 버전에서는 복잡한 ML보다 결정론적 분석 방식을 우선한다.

---

## 저장소 경계 규칙 (Repository Boundary Rules)

- 이 저장소는 모노레포로 운영한다.
- 현재 구현 대상 서비스는 `apps/review-insights/` 아래에만 둔다.
- 다른 팀의 추천 시스템은 별도 앱으로 취급하며, 구현과 데이터 경계를 섞지 않는다.
- `shared/` 공통화는 실제 중복이 검증되기 전까지 하지 않는다.
- 코드, 데이터, 테스트, 문서는 가능한 한 앱 내부에 함께 둔다.

---

## 분석 규칙 (Analysis Rules)

- 리뷰 분석은 한국어 단일 언어 기준으로 수행한다.
- 다음 리뷰는 분석에서 제외하되, 메타데이터는 보존한다.
  - 의미 없는 저품질 리뷰
  - 단순 욕설-only 리뷰
  - 주요 내용이 한국어가 아닌 리뷰
- 전처리는 `결정론적 코어`가 기본이며, LLM은 애매한 리뷰에 대한 `선택적 fallback`으로만 사용한다.
- LLM을 전처리의 기본 엔진으로 사용하거나, 원문 리뷰를 재작성하는 방식은 금지한다.
- 카테고리 분류는 기본적으로 `멀티 라벨`로 처리한다.
- 규칙 기반 판정, LLM 판정, 최종 판정의 흔적을 저장 가능하게 설계한다.
- 키워드/테마 추출은 단순 unigram-only 접근에 고정하지 말고, 정규화된 구문과 대표 표현을 우선 고려한다.
- MVP에서는 단일 우선순위 스코어를 사용하지 않는다.
- 대신 카테고리별 주요 이슈 신호를 설명 가능한 형태로 제공한다.
- `playtime_at_review_hours`, `voted_up`, `num_reviews`는 주요 이슈 신호를 해석하는 보조 메타데이터로 사용한다.
- 외부 응답에 일부 메타데이터가 없을 수 있으므로 `null` 허용 설계를 전제로 한다.

---

## 수정 전 확인 사항 (Before Editing)

- 먼저 저장소 구조를 분석한다.
- 기존 코드 패턴을 파악하고 재사용한다.
- 큰 변경 전에 어떤 파일을 어떻게 수정할지 설명한다.

---

## 출력 기대 사항 (Output Expectations)

- 기능 변경 시 문서를 함께 업데이트한다.
- 필요한 경우에만 명확한 주석을 추가한다.
- 네이밍 규칙을 일관되게 유지한다.
- 새로운 의존성 추가 시 설정 방법을 포함한다.

---

## 테스트 (Testing)

- 가능한 경우 관련 테스트를 실행한다.
- 테스트가 없다면 핵심 로직에 대한 최소 테스트를 추가한다.
- 테스트한 내용과 테스트하지 않은 내용을 명확히 보고한다.
